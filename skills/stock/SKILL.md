---
description: Stock a new method in the global hoard with working code and context
---

Use the Agent tool to launch the `method-hoard:stock` agent with model `opus`. Pass `$ARGUMENTS` as the method details to stock (inline description, file path to extract from, or structured method data).
