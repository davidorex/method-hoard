---
description: Retrieve a specific method from the hoard by id or slug
disable-model-invocation: true
---

Use the Agent tool to launch the `method-hoard:get` agent with model `sonnet`. Pass `$ARGUMENTS` as the method id or slug to retrieve.
